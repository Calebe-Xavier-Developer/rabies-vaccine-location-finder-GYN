export type LocationType = {
  lat: number;
  lng: number;
};

export type VaccinationPoint = {
  name: string;
  address: string;
  latitude: number;
  longitude: number;
  distance: number;
  duration: number;
};
